
# 빌드 도구
빌드 도구란?
소스코드를 실행가능한 애플리케이션으로 바꿔주는 과정
빌드는 컴파일, 패키징, 테스팅 등의 과정을 포함한다.
또한, 라이브러리 버전관리, 의존성관리 등의 기능을 제공한다.

사용하는 이유?
사용하는 이유로는 대규모 프로젝트에서, 빌드를 어떤순서로하고, 테스트를 하고, 또 의존성은 관리는 따로 하고, 이런게 너무 복잡하니까
한번의 과정으로 처리해주어서 편리하게 된다.

## Gradle과 Maven?
빌드도구에는 대표적으로 두가지가 있는데, 나중에 알아봐야겠다.

## Gradle 구성요소
- gradlew, gradlew.bat
    - gradlew는 gradle을 설치하지 않아도, 프로젝트를 빌드할 수 있게 해주는 **스크립트 파일**이다.
    - gradlew는 MAC, 리눅스 환경이고, gradlew.bat은 Window환경이다.
    - 완벽하게 설치한다기보다는 임시 저장소인 캐시에 두고, 꺼내쓰는 방식으로 작동함.
    - 해당 프로젝트 내에서만 작동하는 것임.

- gradle/wrapper 폴더
    - gradlew 스크립트가 작동할 수 있게 필요한 파일들이 있는 폴더
        - gradle-wrapper.jar: Gradle을 다운로드하고 실행하는 실제 자바 실행 파일
        - gradle-wrapper.properties : 다운로드할 gradle의 버전 정보들이 적혀있는 파일
- settings.gradle
    - 프로젝트의 전체적인 구성 정보를 담고 있는 파일
    - 프로젝트의 이름을 지정하고, 큰 프로젝트라면, 어떤 모듈들이 이 프로젝트에 포함되는지 정의함.
- build.gradle
    - 핵심 빌드 스크립트
    - 의존성
        - 코드 묶음인 라이브러리들을 땡겨옴.
    - 플러그인
        - 자바 컴파일, 스프링 부트 등, 실행에 돕는 플러그인들을 선언함.
    - 작업
        - 컴파일
        - 패키징
        - 테스팅
- .gradle
    - gradle 숨김 폴더
    - 작업 및 캐시 폴더, 이전에 빌드 했던 것들을 저장해둬서 나중에 더 빨리 처리하게끔함.
    - 보통 git에 올리지 않음

## Properties와 Yml의 차이
두개 모두 스프링부트 애플리케이션의 설정을 담고 있는 파일이다.  
이미 build 파일에서 받아온 라이브러리상의 폴더들에서 어떤 부분을 갖다 쓸 지 설정하는 부분이다.
src/main/resources 경로에 존재한다.

- Properties
    - Key=Value 꼴
    - 들여쓰기를 지원하지 않음
- Yml
    - Key:Value 꼴
    - 들여쓰기를 지원하여, 계층형으로 볼 수 있음.

- 두 파일을 같이 쓰게 되면 우선순위가 Properties가 더 높기 때문에, yml이 덮어씌어진다. 따라서, 둘 중 한가지만 사용하는 것이 좋다.
출처 : https://colabear754.tistory.com/90 


## innoDB란?
mysql 데이터베이스 엔진


# 어노테이션 
## @Entity

## @id

## GeneratedValue()



## SQL
- use (데이터베이스 명);
- describe (테이블 명);



### port spring은 보통 설정 안하면 8080 서버다.





## JPA(jakarta persistence api, 구 Java Persistence api)_미완성
DB는 스키마 기반이고, java는 객체지향 코드라서, 둘이 패러다임이 다름.
JPA는 SQL을 직접 작성하기보다 엔티티와 매핑 정보만 정의해두면 CRUD SQL을 자동으로 생성하고 실행해주는 방식의 표준 ORM 기술입니다.
https://coding-factory.tistory.com/1172 
https://ride-dev.tistory.com/105 


## HTTP 메세지 구조.


## 예외 처리 종류?
- 체크 예외
- unchecked 예외
IllegalArgumentException


## 빌드란? .build()

