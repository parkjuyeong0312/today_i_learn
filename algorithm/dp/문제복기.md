### 1로 만들기
https://www.acmicpc.net/problem/1463 
```
정수 X에 사용할 수 있는 연산은 다음과 같이 세 가지 이다.

X가 3으로 나누어 떨어지면, 3으로 나눈다.
X가 2로 나누어 떨어지면, 2로 나눈다.
1을 뺀다.
정수 N이 주어졌을 때, 위와 같은 연산 세 개를 적절히 사용해서 1을 만들려고 한다. 연산을 사용하는 횟수의 최솟값을 출력하시오.
```

#### 사고 흐름
정방향에서 가는게 낫나 역방향에서 가는게 낫나?
- dp[i]를 채울때, 이전에 채운 값들이 있어야 된다. 
    - dp[i/3], dp[i/2], dp[i-1]

- 테이블 정의
    - dp[i] = i에서 1까지의 최소 연산 횟수
    - 구하는 답 : dp[X] // X에서 1까지의 최소 연산 횟수
- 점화식 정의
```
i = 2, i<=N, i++
    dp[i] = Math.min(dp[i],dp[i-1]+1)
    if(i%3==0){
        dp[i] = Math.min(dp[i], dp[i/3]+1) //역방향이라고 했을때,
    }
    if(i%2 ==0){
        dp[i] = Math.min(dp[i], dp[i/2]+1)
    }
```
- 초기값 설정
dp[1]==0


## 1,2,3 더하기
https://www.acmicpc.net/problem/9095 
```
정수 4를 1, 2, 3의 합으로 나타내는 방법은 총 7가지가 있다. 합을 나타낼 때는 수를 1개 이상 사용해야 한다.
1+1+1+1
1+1+2
1+2+1
2+1+1
2+2
1+3
3+1
정수 n이 주어졌을 때, n을 1, 2, 3의 합으로 나타내는 방법의 수를 구하는 프로그램을 작성하시오.
```

#### 사고흐름
문제 정의부터 해보자. 
정수 4를 1,2,3의 합으로 나타내는 방법의 수

- 테이블 정의
    - dp[i] = 정수 i를 1,2,3의 합으로 나타내는 방법의 수 
- 점화식 정의
```
    i=4, i<target, i++
    dp[i] = dp[i-1]+dp[i-2]+dp[i-3]
    마지막에 1을 더한거 + 2를 더한거 + 3을 더한거
```

- 초기값 정의 
    - dp[0]=1 
    - dp[1]=1
    - dp[2]=2
    - dp[3]=4


## 계단오르기
https://www.acmicpc.net/problem/2579 
```
계단은 한 번에 한 계단씩 또는 두 계단씩 오를 수 있다. 즉, 한 계단을 밟으면서 이어서 다음 계단이나, 다음 다음 계단으로 오를 수 있다.
연속된 세 개의 계단을 모두 밟아서는 안 된다. 단, 시작점은 계단에 포함되지 않는다.
마지막 도착 계단은 반드시 밟아야 한다.
따라서 첫 번째 계단을 밟고 이어 두 번째 계단이나, 세 번째 계단으로 오를 수 있다. 하지만, 첫 번째 계단을 밟고 이어 네 번째 계단으로 올라가거나, 첫 번째, 두 번째, 세 번째 계단을 연속해서 모두 밟을 수는 없다.

각 계단에 쓰여 있는 점수가 주어질 때 이 게임에서 얻을 수 있는 총 점수의 최댓값을 구하는 프로그램을 작성하시오.
```
<문제분석>  
경우의 수 : 한계단, 두계단 갈 수 있음. 연속된 세계의 계단을 모두 밟으면 안됨.
"X번째 계단에 올랐을때, 최댓값"

//연속된 부분에 대한 처리는 어케하지?
//2차원배열로 처리해야되나?

//그리고 마지막에 dp[X][0], dp[X][1], dp[X][2] 중에 max값 지정.
- 테이블 정의
dp[i][0] : i번째 계단이 연속 1번째, i번째 계단을 밟았을 때 최대점수
dp[i][1] : i번째 계단이 연속 2번째, i번째 계단을 밟았을 때 최대점수
- 점화식 정의
dp[i][0] = max(dp[i-2][0],dp[i-2][1]) + stair[i] // 두칸 점프해서 올라와서 첫번째 카운트.
dp[i][1] = dp[i-1][0] + stair[i]
- 초기값 정의

dp[1][0] = stair[1]
dp[1][1] = 0

dp[2][0] = stair[2]
dp[2][1] = stair[1] + stair[2]


## RGB 거리
https://www.acmicpc.net/problem/1149 
```
RGB거리에는 집이 N개 있다. 거리는 선분으로 나타낼 수 있고, 1번 집부터 N번 집이 순서대로 있다.

집은 빨강, 초록, 파랑 중 하나의 색으로 칠해야 한다. 각각의 집을 빨강, 초록, 파랑으로 칠하는 비용이 주어졌을 때, 아래 규칙을 만족하면서 모든 집을 칠하는 비용의 최솟값을 구해보자.

1번 집의 색은 2번 집의 색과 같지 않아야 한다.
N번 집의 색은 N-1번 집의 색과 같지 않아야 한다.
i(2 ≤ i ≤ N-1)번 집의 색은 i-1번, i+1번 집의 색과 같지 않아야 한다.
```

<문제분석>
어쨋든 이웃하는 부분이 같지 않으면되는 부분이다.
빨강, 초록, 파랑으로 경우의 수가 나뉜다.
따라서, 빨간색을 골랐다고 했을때에는 초록,파랑밖에 못고르고, 뭐 그런식이니까
dp[i][3] 으로 두고 해야겟다
0 , 1, 2 순으로 빨 초 파 순으로


- 테이블 정의 
dp[i][j] j색을 골랐을때, i번째 집에서의 최소 비용
- 점화식 정의
dp[i][0] = Math.min(dp[i-1][1],dp[i-1][2])+colors[i][0]
... 이런식으로 진행
- 초기값 정의
dp[0][0], dp[0][1], dp[0][2] 를 정의한다.


